# Quick Development Commands

## 🔥 Most Used

```bash
# Complete fresh start (deletes everything)
make ENV=dev fresh-start

# Rebuild after requirements.txt changes
make ENV=dev rebuild

# Start development server
make ENV=dev start

# Restart after code changes
make ENV=dev restart

# View logs
make ENV=dev logs
```

## 📖 Full Documentation

- **Reset Guide:** `docs/DEVELOPMENT_RESET_GUIDE.md`
- **Requirements:** `docs/REQUIREMENTS_MANAGEMENT.md`
- **All Commands:** `make help`

## ⚠️ Important

- **Only ONE requirements.txt:** `./requirements.txt` (project root)
- **After editing requirements.txt:** Run `make ENV=dev rebuild`
- **Never use:** `docker exec ... pip install` (changes don't persist)

---

Remember: Always use `ENV=dev` for development commands!
