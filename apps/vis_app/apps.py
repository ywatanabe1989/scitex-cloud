from django.apps import AppConfig


class VisAppConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.vis_app"
    verbose_name = "Scientific Figure Editor (Vis)"
